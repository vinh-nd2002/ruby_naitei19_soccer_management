<% if @favorite_pitches.present? %>
  <h3 class="text-indigo-500 my-10 font-bold text-4xl container">
    <%= t "users.favorite_pitches"%>
  </h3>
  <div class="container grid grid-cols-4 gap-4">
    <%= render @favorite_pitches %>
  </div>
  <div class="flex justify-center items-center my-5">
    <%== pagy_bootstrap_nav(@pagy_favorite) %>
  </div>
<% end %>
